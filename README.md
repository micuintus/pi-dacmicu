# pi-dacmicu

Deterministic TODO loop for [Pi](https://pi.dev).

Install:
```bash
pi install git:github.com/micuintus/pi-dacmicu
```

Requires `tintinweb/pi-manage-todo-list`:
```bash
pi install git:github.com/tintinweb/pi-manage-todo-list
```

Run tests:
```bash
npm test
```
